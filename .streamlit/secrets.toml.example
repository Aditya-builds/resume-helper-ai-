# .streamlit/secrets.toml.example
# Copy this file to `.streamlit/secrets.toml` for local testing (do NOT commit the real file)

[general]
# Example OpenAI API key entry
OPENAI_API_KEY = "sk-REPLACE_WITH_YOUR_KEY"

# You can also add other secrets here (database URLs, third-party tokens, etc.)
